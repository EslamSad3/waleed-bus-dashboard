# Server-only — never prefix with NEXT_PUBLIC_* (would leak to the client bundle)
BUS_API_URL=http://localhost:3000
DASHBOARD_ORIGIN=http://localhost:3000
SESSION_COOKIE_SECRET=change-me-to-a-long-random-string
PDF_FONT_PATH=./assets/fonts/Cairo-Variable.ttf
NEXT_PUBLIC_APP_NAME=منصة الأتوبيسات
